
/**
 * @deprecated Use the new adapter at src/adapters/priceItemAdapter.ts instead
 */
import { adaptPriceItems } from "@/adapters/priceItemAdapter";

// Re-export for backward compatibility
export { adaptPriceItems };
